# Deployment Templates

This folder provides deployment templates for:

- `systemd` (Linux persistent process)
- `launchd` (macOS persistent process)
- `cloudflare` (Workers + D1 serverless)
- `vercel` (Functions + Postgres serverless)

Use module-level instructions from [../README.md](../README.md).
